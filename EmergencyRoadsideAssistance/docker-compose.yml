version: '3.4'

services:
    api:
        build:
          context: .
          dockerfile: Dockerfile
        ports:
          - "8080:80"
        depends_on:
          - db

    db:
        image: postgres
        restart: always
        environment:
            POSTGRES_PASSWORD: example